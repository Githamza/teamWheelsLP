.DS_Store
Thumbs.db

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Hugo (generated / cache)
public/
resources/_gen/
.hugo_build.lock
hugo_stats.json

# Editor / tooling
.cursor/

# Google Search Console credentials & data
scripts/search-console/tokens.json
scripts/search-console/client_secret.json
scripts/search-console/data/

# Web Bot Auth — private signing keys (public JWKS lives under static/.well-known/)
.secrets/

# Agent / skill tooling (installed locally, not part of the site)
# Note: .claude/commands/ is tracked — only ignore the locally-installed skills.
.agents/
.claude/skills/
skills-lock.json

